# Empliq Scraper API - Variables de entorno

# ─── Seguridad ───
# API Key para autenticación (header: x-api-key)
# Generar con: openssl rand -hex 32
API_KEY=tu-api-key-aqui

# ─── Puerto del microservicio ───
SCRAPER_PORT=3457

# ─── Límites por sesión ───
DDG_HTTP_MAX=200
PUPPETEER_MAX=80
PLAYWRIGHT_MAX=50

# ─── Delays entre búsquedas (ms) ───
DDG_HTTP_DELAY_MIN=2000
DDG_HTTP_DELAY_MAX=5000
PUPPETEER_DELAY_MIN=5000
PUPPETEER_DELAY_MAX=10000
PLAYWRIGHT_DELAY_MIN=15000
PLAYWRIGHT_DELAY_MAX=30000
